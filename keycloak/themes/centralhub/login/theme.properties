parent=keycloak
import=common/keycloak

# Load the parent theme's own login.css first (base layout/functionality),
# then ours last so it can override without duplicating any of Keycloak's
# base styles or touching templates — CSS-only, so form behavior can't break.
styles=css/login.css css/centralhub.css
